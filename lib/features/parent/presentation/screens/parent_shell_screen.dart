import 'package:flutter/material.dart';

/// Placeholder parent shell screen.
///
/// This is a temporary stub that will be replaced by the full
/// parent dashboard shell once that feature is implemented.
class ParentShellScreen extends StatelessWidget {
  const ParentShellScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Parent Shell – stub')),
    );
  }
}
